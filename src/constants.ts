export const messages = {
    SUCCESS: "success",
    FAILED: "failed",
}

export enum Tables {
    Task = 'task',
    TaskList = 'tasklist',
    TaskAssignment = 'taskassignment' //to save references to tasks and task lists (re)assignments
}